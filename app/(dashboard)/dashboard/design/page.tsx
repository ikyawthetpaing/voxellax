import { Metadata } from "next";

export const metadata: Metadata = {
  title: "Design",
  description: "Design your store",
};

export default function DashboardDesignPage() {
  return <div>DashboardDesignPage</div>;
}
